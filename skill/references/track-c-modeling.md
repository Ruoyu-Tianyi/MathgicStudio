# C 赛道（数据驱动）方法论

P0 判定为 C 型后，本文件是建模阶段的**主参考**；`data-analysis-workflow.md` 管流程纪律，本文件管方法深度。`problem-types.md` 只做快速路由，确定方向后回到本文件查实现细节与检验要求。

## 目录
- [运行环境探针与降级映射](#运行环境探针与降级映射)
- [总决策树](#总决策树)
- [评价类：权重 → 合成 → 排序](#评价类权重--合成--排序)
- [预测类：时序与回归](#预测类时序与回归)
- [统计推断类：相关 / 显著性 / 因果表述](#统计推断类相关--显著性--因果表述)
- [数据驱动优化类](#数据驱动优化类)
- [成分数据（Compositional Data）纪律](#成分数据compositional-data纪律)
- [C 题灵敏度与检验清单](#c-题灵敏度与检验清单)
- [论文写法要点](#论文写法要点)

## 运行环境探针与降级映射

开工先探一次环境，再定模型实现路径：

```python
for m in ["scipy", "sklearn", "statsmodels"]:
    try: __import__(m); print(m, "OK")
    except ImportError: print(m, "MISSING")
```

Kimi Work managed runtime **无 scipy / sklearn / statsmodels**——此时禁止按教科书直接写 `from sklearn...`，改用 `code/stats_utils.py`（scaffold 自带，含分布生存函数/卡方/Mann-Whitney/Spearman/FDR/K-Means/ARI/CLR/决策桩/贝叶斯回归/GMM 软聚类，数值已对标准表校验）。降级映射：

| 教科书方案 | 无 sklearn 时的替代 | 代价/收益 |
|---|---|---|
| scipy.stats 各检验 | `stats_utils` 同名函数 | 无代价（已校验） |
| 随机森林/XGBoost 分类 | 决策桩/浅层 Gini 树 + 最近质心双证据 | 更可解释，小样本反而更稳 |
| sklearn KMeans | `stats_utils.kmeans`（k-means++，n_init） | 无代价 |
| 特征重要性（MDI/SHAP） | F 比 / Gini 下降 / 删特征 ARI 变化 | 论文中说明度量口径 |
| LSTM/深度模型 | 不用——无框架且 C 题样本量通常不够 | 本来就该慎用 |
| 优化求解器（scipy.optimize） | 手写网格/坐标下降/牛顿迭代 | 小规模问题足够 |

**检验要求不变**：无论用哪条路径，交叉验证、显著性、灵敏度一项不能少。

## 总决策树

```
数据形态？
├─ 截面/面板 + "评价/排序/综合"     → 评价类（§2）
├─ 时间序列 + "预测/趋势"           → 预测类-时序（§3.1）
├─ 多特征样本 + "预测/估计/影响因素" → 预测类-回归 / 统计推断（§3.2 / §4）
├─ 无标签 + "分类/聚类/画像"        → 聚类 + 画像描述（§4.4）
└─ 有目标函数 + "定价/补货/调度"     → 数据驱动优化（§5）
```

判不清时看动词：题目让你"给出方案/决策"→ 优化；让你"打分/排名"→ 评价；让你"找规律/关系"→ 统计推断。

## 评价类：权重 → 合成 → 排序

### 决策树

```
权重来源？
├─ 纯主观（专家打分）     → AHP（必做一致性检验 CR<0.1）
├─ 纯客观（数据变异程度） → 熵权法（信息熵）/ CRITIC（含冲突性）
└─ 组合                  → AHP 主客观组合权重 w = α·w_AHP + (1-α)·w_熵，α 灵敏度扫描

排序方法？
├─ 指标可向量化为"距理想解远近" → TOPSIS（先正向化 + 归一化）
├─ 指标间有隶属度模糊性         → 模糊综合评价
└─ 指标量纲差异大且要保序       → 灰色关联分析
```

### 关键公式（论文必须写全推导链）

**熵权法**：对第 j 个指标，$p_{ij} = x'_{ij} / \sum_i x'_{ij}$（$x'$ 为归一化值），$e_j = -\frac{1}{\ln n}\sum_i p_{ij}\ln p_{ij}$，$w_j = (1-e_j)/\sum_j(1-e_j)$。注意 $\ln 0$ 处理：$p_{ij}=0$ 时该项按 0 计。

**TOPSIS**：正理想解 $A^+$、负理想解 $A^-$，贴近度 $C_i = D_i^-/(D_i^+ + D_i^-)$。指标先做**正向化**（成本型 → 效益型：$x' = \max - x$ 或 $1/x$，论文中说明选用及理由）。

**AHP 一致性**：$CI = (\lambda_{\max}-n)/(n-1)$，$CR = CI/RI < 0.1$；不满足必须调整判断矩阵并在论文中说明调整方式。

### 检验（缺一扣分）

- 权重扰动 ±10% 重排，报告排名翻转数（用 `scripts/sensitivity.py` 的 `sweep()`）
- 主模型 vs 基线（如熵权 TOPSIS vs 等权 TOPSIS）排名 Spearman 相关系数
- AHP 判断矩阵来自"文献 + 数据特征"双重依据，不许凭空填 1/3/5/7

## 预测类：时序与回归

### 时序路线（单序列）

```
ADF 平稳性检验
├─ 平稳        → ARIMA(p,d,0)，AIC/BIC 定阶，残差 Ljung-Box 白噪声检验
├─ 不平稳      → 差分后重来（d=1 不够再 d=2，很少需要）
├─ 强季节性    → SARIMA 或 Holt-Winters
└─ 数据 <30 点 → 灰色 GM(1,1)：级比检验 → 建模 → 残差/后验差比 C<0.35
```

深度学习（LSTM/GRU）仅当样本量 >500 且有明确非线性证据时使用，且必须配 ARIMA 基线对比；样本不够用了就是减分项。

### 回归路线（多特征）

1. **先做共线性**：VIF > 10 的变量剔除或合并（论文中列 VIF 表）
2. **主模型选择**：
   - 关系近似线性、要解释系数 → 多元线性回归（报系数 + t 检验 p 值 + 置信区间）
   - 非线性/交互复杂、要精度 → 随机森林 / XGBoost（报特征重要性 + SHAP 摘要图更佳）
3. **验证纪律**：时序数据按时间切分（禁随机划分防泄漏）；截面数据用 5 折交叉验证，报均值 ± 标准差
4. **指标**：RMSE / MAE / MAPE / R² 至少报三个；分类问题报 F1（不平衡数据先报类别占比）

### 检验

- 残差诊断四图（残差 vs 拟合值、Q-Q、残差直方图、残差时序）放附录
- 灵敏度 = 训练窗口长度 / 超参数（如树深度）扰动，报指标波动范围

### 贝叶斯不确定性量化（深刻档）

点估计只给"预测值"，贝叶斯给"预测值 + 逐样本可信区间"——这是"扎实档"与"深刻档"的分水岭，尤其适合**个体化预测**（如逐件文物反演风化前成分）。

- **工具**：`stats_utils.bayes_linreg(X, y)`（NIG 共轭，闭式解，纯 numpy）+ `bayes_predict(post, Xnew, level=0.95)`；弱信息先验下后验均值≈OLS，不引入额外假设风险
- **截距模型 = 类均值的贝叶斯版**：X 只放截距列，就是把"同类均值预测"升级为带区间的正态均值推断，零建模成本完成升级
- **先验纪律**：默认弱先验（V0=1e6·I）起步；使用信息先验时必须做**先验灵敏度**（换一组先验重算，结论不变才写进正文）
- **检验写法**：留一/留出法报告**区间覆盖率**——名义 95% 区间实测命中应≈0.95（2022 C 题玻璃数据实测：高钾 0.935、铅钡 0.923，小样本成分数据下达标）；覆盖率显著偏低说明模型误设，要回到残差诊断
- **论文呈现**：预测表从"成分 | 预测值"升级为"成分 | 预测值 | 95% 可信区间"，配误差棒图；先验灵敏度写进检验节

## 统计推断类：相关 / 显著性 / 因果表述

- **相关选择**：正态连续 → Pearson；非正态或等级 → Spearman；类别-类别 → 卡方 + Cramér's V
- **显著性**：两组 → t 检验（先 Shapiro 正态性）；多组 → ANOVA + 事后 Tukey；非正态换 Mann-Whitney / Kruskal-Wallis
- **表述红线**：观察性数据只写"显著相关（r=…, p<0.05）"，禁止"导致/促进/提升"；确需因果方向时写"与……一致，提示可能的作用方向"
- **多重检验**：同时检验 >10 个假设时提一句 Bonferroni/FDR 校正
- **相关结构差异检验（Fisher z）**：比较两组相关系数 $\rho_1$ vs $\rho_2$ 时，不得只凭 $|\Delta\rho|$ 大小下"差异显著"结论——用 Fisher z 变换（$z=(\mathrm{atanh}\,\rho_1-\mathrm{atanh}\,\rho_2)/\sqrt{1/(n_1-3)+1/(n_2-3)}$）算 p 值，多对比较再 FDR 校正；校正后不显著则如实定位为"描述性证据：差异方向稳定、机理自洽，但显著性需更多样本"（2022 C 题实测：91 对 $|\Delta\rho|$ 最大 0.80，FDR 后无一过 0.05，保守定位反而成为严谨性加分点）
- **检验功效（power）**：主检验报告实测效应量下的功效（卡方用 $w$、两样本用 $\delta/\sigma$）；功效 <0.8 时给出达标样本量估计——"样本量扩至约 N 件即可达标"比"样本量不足"四个字有信息量得多；功效不足的边缘结论全部降格为保守表述

### 聚类与画像

- K-Means：肘部法 + 轮廓系数双证据定 k；特征必须先标准化
- **软聚类路线（GMM，深刻档）**：`stats_utils.gmm()` EM 实现 + `gmm_select()` BIC 选 k，输出每样本**后验归属概率**：
  - **协方差纪律**：`cov_type="diag"` 是小样本默认（n ≲ k·d²/2 时全协方差过参数化，BIC 会选出病态解）；样本充足才用 `"full"`
  - **过渡样本量化**：max 后验 <0.7 的样本是"边界样本"，单列成表报告——比"存疑"定性词有力得多
  - **第三证据角色**：当规则法与距离法结论冲突（如 2022 C 题 A5：规则→铅钡、质心→高钾），GMM 后验是独立的第三方量化裁决（实测 A5 后验 1.000 支持规则判定，分歧源于质心法受风化漂移影响）
  - **BIC vs 轮廓系数**：两者选 k 可能不同（BIC 测分布拟合、轮廓系数测几何分离），不同时如实并列报告并说明采纳理由，这本身就是检验节素材
  - **后验概率警示**：GMM 概率是模型条件概率，分离良好时趋近 0/1（过度自信），只能当相对证据，不得写成"该样本有 99.9% 概率属于 X 类"
- **小簇纪律**：样本数 <5 的簇不得作为正式亚类结论——并入相邻簇或如实表述为"边界离群组"，并在敏感性分析中报告其归属稳定性
- 聚类后必做**画像表**：每类的特征均值对比 + 业务命名（如"高值低频型"），这是 C 题论文的加分点
- **画像图只画关键成分**：热力图/雷达图仅展示用于聚类的成分或类内 SD Top N（全成分里的近零检出列只会稀释信息量）
- 检验：不同 seed 下聚类稳定性（ARI 系数 >0.8 视为稳定）；删除 Top1 特征后 ARI 变化作为敏感性证据；KMeans 与 GMM 同 k 标签 ARI 偏低（<0.5）时提示亚类结构本身不稳，配合小簇纪律降 k 或弱化结论
- **阈值规则的解析互验（V4）**：搜索/决策桩得到经验阈值后，用两条独立路径核验其必然性——① **实测类间间隙**：$[\max_{\text{类A}} x,\ \min_{\text{类B}} x]$ 构成不敏感区间，区间内任何阈值都等价完美分离，报告区间宽度与经验阈值的相对位置 ② **高斯解析边界**：两类分别拟合 $N(\mu_1,\sigma_1^2)$、$N(\mu_2,\sigma_2^2)$，等先验 QDA/Bayes 最优边界解 $\frac{(x-\mu_1)^2}{\sigma_1^2}-\frac{(x-\mu_2)^2}{\sigma_2^2}=2\ln\frac{\sigma_2}{\sigma_1}$ 的居间根，给出理论错误率；方差悬殊（$\sigma_2/\sigma_1$ 大）时 LDA 中点判据 $(\mu_1+\mu_2)/2$ 失效——失效原因写透就是模型建立节的深度素材
- **分类鉴别三证据投票（V4）**：待鉴别样本用三路独立判据——规则（可解释阈值）+ 距离（标准化最近质心）+ 协方差结构（LDA/Fisher 判别，判别函数 $\delta_c(\mathbf{u})=\mathbf{u}^\top\hat\Sigma^{-1}\boldsymbol\mu_c-\frac12\boldsymbol\mu_c^\top\hat\Sigma^{-1}\boldsymbol\mu_c+\ln\hat\pi_c$，留一交叉验证报错误率）；三法一致或 2:1 出结论，分歧件判"存疑"并逐条写清各判据依据；分层 bootstrap（B=500 类内有放回重抽样 → 重拟合 → 再预测）给出每件的归属后验频率，作为概率化置信度

## 数据驱动优化类

C 题优化与 B 题优化的区别：**参数来自前面的统计分析**，不是题目直接给的。论文必须画出"数据 → 参数估计 → 优化模型 → 决策"的完整链条（用 `plot_setup.flow()`）。

- 需求/供给是估计出来的分布 → 随机规划或蒙特卡洛仿真优化（抽 N=1000 个场景求期望最优）
- 价格弹性、成本系数 → 回归估计，论文中给出估计区间，优化结果对区间端点各解一次（稳健性证据）
- 多目标（利润 vs 风险 vs 满意度）→ 加权法 + 权重灵敏度，或 Pareto 前沿图

## 成分数据（Compositional Data）纪律

化学成分百分比、配料比、支出结构等"各分量加和 ≈100%"的数据有特殊陷阱，处理不当结论会系统性偏掉：

- **空白语义先确认**：成分表的空白通常 = "未检测到"（应为 0），不是缺失值——用 `eda.py --blank-as-zero` 探查，论文中显式声明该处理
- **行和有效性**：赛题常给有效区间（如累加和 85%~105%），用 `--row-sum-range` 检出无效行并在预处理节说明剔除
- **闭合效应（spurious correlation）**：分量此消彼长导致相关分析失真——两个成分即使独立也会因"总和恒定"呈负相关。对策按深度三选一：
  1. 最低限度：论文中声明闭合效应存在，相关结论限定为"描述性关联"
  2. 标准做法：CLR 变换（$clr(x)_i = \ln(x_i / g(x))$，g 为几何均值）后再算相关/聚类/PCA
  3. 含 0 分量：CLR 前 0 值替换为小正数（如最小检出限的一半），说明替换规则
- **亚类划分/聚类**：优先在 CLR 空间做欧氏距离聚类；直接用原始百分比要说明理由
- **"反演/还原"类问题**（如预测风化前成分）：用比例关系而非绝对量建模（成分比迁移），结果再归一化回 100%

## C 题灵敏度与检验清单

| 环节 | 必做项 |
|---|---|
| 数据预处理 | 异常值规则改变（3σ → IQR）重跑主结果，报告是否翻转 |
| 评价 | 权重 ±10%、α 组合系数 0~1 扫描、方法间 Spearman 相关 |
| 预测 | 训练窗口扰动、超参扰动、与基线模型对比 |
| 优化 | 关键估计参数 ±10%~20% 重解（`sensitivity.py` 的 `sweep_multi()` + `tornado()`） |

## 论文写法要点

- EDA 发现必须**编号列表化**（"发现 1：……"），模型选择段落逐一回引（"针对发现 2 的非线性特征，选用……"）
- 每个模型一节内结构固定：**方法原理（含公式推导）→ 数据代入 → 结果 → 检验 → 小结**
- 三线表呈现：权重表、VIF 表、模型对比指标表、灵敏度结果表；图呈现：EDA 四图、Pareto 前沿、扰动曲线、聚类画像雷达图
- 灵敏度分析单独成节（通常放在"模型检验"或"稳健性分析"），有表有图有结论，不许一句话带过
